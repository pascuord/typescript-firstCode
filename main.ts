// Crea una función que imprima un mensaje en la consola
function imprimirMensaje() {
  const mensaje: string = '¡Hola, mundo!';
  console.log(mensaje);
}

// Llama a la función para imprimir el mensaje
imprimirMensaje();

